package com.interviewquestions;

public class MaxSumPathInMatrix {
	public static void main(String args[]) {
		/*
		 * Given a N X N matrix Mat[N][N] of positive integers. There are only
		 * 2 possible moves from a cell (i, j)
		 * 
		 * 1). (i+1, j) 
		 * 2). (i, j+1)
		 * Starting from [0][0], return the largest sum of any of the paths up to [N-1][N-1]
		 */
	}
}
